# ============================================================
# Student Analytics - 环境变量配置示例
# 使用说明：复制此文件为 .env 并根据实际情况修改配置
# ============================================================

# --------------------------------------------------------
# 端口配置
# --------------------------------------------------------
# MySQL 对外端口（默认 3307，避免与本地 MySQL 冲突）
MYSQL_PORT=3307

# MongoDB 对外端口
MONGO_PORT=27017

# 应用对外端口
APP_PORT=8090

# --------------------------------------------------------
# 数据库配置
# --------------------------------------------------------
# MySQL root 密码
MYSQL_ROOT_PASSWORD=root123

# 应用连接 MySQL 的用户名
DB_USERNAME=root

# 应用连接 MySQL 的密码
DB_PASSWORD=root123

# --------------------------------------------------------
# OpenAI 配置（可选，用于 AI 功能）
# --------------------------------------------------------
# 如果不需要 AI 功能，可以留空或使用占位符
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com